target_driver:
  ros__parameters:
    speed: 0.5              # m/s along the circuit
    control_rate: 20.0      # Hz
    loop: true              # restart the circuit after the last waypoint
    goal_tolerance: 0.3     # m, waypoint reached
    k_heading: 1.5          # P gain on heading error
    max_angular: 1.5        # rad/s
    slow_angle: 0.8         # rad, turn in place above this heading error
    map_frame: map
    # map-frame circuit through the corner keep-out zones (obstacles.yaml)
    waypoints: [6.0, 6.0, -6.0, 6.0, -6.0, -6.0, 6.0, -6.0]

map_loader:
  ros__parameters:
    frame_id: map

global_planner:
  ros__parameters:
    replan_rate: 2.0         # Hz
    inflation_radius: 0.4    # m, grown around every occupied cell
    map_frame: map
    robot_frame: base_link
    # Phase 3 stand-in for target_predictor (Phase 4); opposite corner from
    # the robot's default spawn in plan_to_goal.launch.py.
    goal_x: 8.0
    goal_y: 8.0